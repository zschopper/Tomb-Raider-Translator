using System;
using System.Collections.Generic;
using System.Text;
using System.Xml;

namespace TRTR.Processors
{
/*
    class FontFileIntf : ITransProc {
        string ITransProc.Name { get { return "sample"; } }
        void ITransProc.Initialize() { }
        string[] ITransProc.GetFileList() { return new string[0]; }
        void ITransProc.Extract(string dir) { } // extracts translatable data
        void ITransProc.CreateTranslation(XmlNode node, string dir) { } // creates translation xml
        void ITransProc.CreateRestoration(XmlNode node) { } // creates restoration xml
        void ITransProc.Translate(XmlNode node, bool simulated) { } // translates game
        void ITransProc.Restore(XmlNode node, bool simulated) { } // restores game
    }
*/
}
